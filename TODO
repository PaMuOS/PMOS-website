frontend

- uglify
- rhizome client file should be built with gulp. Otherwise we need to start the backend to build the file which is weird


backend

- if no connection on OF side? Store data? Maybe a small local queue
